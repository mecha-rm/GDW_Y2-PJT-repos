#include "PrimitiveCapsule.h"
#include "PrimitiveSphere.h"

cherry::PrimitiveCapsule::PrimitiveCapsule(float cylRadius, float height, unsigned int cylSegments, float capRadius, unsigned int capSegments)
{
	// PrimitiveSphere tempSphere(cylRadius, (capSegments + 1) * 2, cylSegments);


}
